# fetch all remote branches:
-> git fetch origin +refs/heads/*:refs/remotes/origin/*

git reset --hard 
-> remove all unstaged changes

git clean -fd 
-> remove untracked files

git reset --hard origin/<branch_name> 
-> will remove all local changes(including local commits) and roll back to the previous remote commit



-- STASH CHANGES AND SWITCH TO ANOTHER BRANCH --

git stash push -m "temp work"

git checkout develop

git pull origin develop

git checkout -b feat/new-branch-name

git stash pop

git add .

git commit -m '<commit_message>'

git push -u origin <new_branch_name> or git push -u origin head (head will push the local current branch name to the origin)


git clean -fd ---> removes the new files(don't know if repositories as well)

git restore . ----> remove changes made to a directory



########
git stash           # save uncommitted changes
git pull            # update branch with remote changes
git stash pop       # reapply your saved changes






